const generate = async (finalProduct, amount, finalRecipe) => {
  const plan = {
    product: finalProduct,
    amount,
    recipe: finalRecipe,
  };
};

const plan = {
  product: "AI Limiter",
  amount: 100,
  Recipe: "AI Limiter",
  alternateRecipes: [],
  buildings: 20,
  ingredients: [{}, {}],
};
